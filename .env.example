# Copy to .env and fill in. Never commit .env.
# Required for sentiment.py (Claude sentiment labeling).
ANTHROPIC_API_KEY=sk-ant-...

# Optional — only needed if using the Alpaca data source instead of yfinance.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_ENDPOINT=
